{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PedidoProducto</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pedido</th>
                <td>{{ entity.pedido }}</td>
            </tr>
            <tr>
                <th>Producto</th>
                <td>{{ entity.producto }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ entity.cantidad }}</td>
            </tr>
            <tr>
                <th>Valortotal</th>
                <td>{{ entity.valorTotal }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ entity.observaciones }}</td>
            </tr>
        </tbody>
    </table>

        <div class="form-actions">
        <a href="{{ path('pedidoproducto') }}" class="btn btn-mini">
            Cancelar        </a>
        <a href="{{ path('pedidoproducto_edit', { 'id': entity.id }) }}" class="btn btn-primary btn-mini">
            Editar
        </a>
{{ form(delete_form) }}
{% endblock %}
