{% extends '::base.html.twig' %}
{% block stylesheets%}
<style>
    table tr th{width:100px;}
</style>
{%endblock%}
{% block body -%}
    <!-- row -->
    <div class="row">

        <!-- col -->
        <div class="col-xs-12 col-sm-7 col-md-7 col-lg-4">
            <h1 class="page-title txt-color-blueDark"><!-- PAGE HEADER --><i class="fa-fw fa fa-edit"></i> Editar Compa&ntilde;ia / Cliente</h1>
        </div>
        <!-- end col -->
        <!-- right side of the page with the sparkline graphs -->
    </div>
    <!-- end row -->
    {{ form_start(edit_form,{'attr':{'class':'form-inline'}}) }}
        <table class="table table-condensed">
            <tr>
                <th>Fecha Ingreso</th>
                <td>{{form_widget(edit_form.fechaIngreso)}}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{form_widget(edit_form.estado)}}</td>
            </tr>
            <tr>
                <th>Cliente</th>
                <td>{{form_widget(edit_form.cliente)}}</td>
            </tr>
            <tr>
                <th>Compañia</th>
                <td>{{form_widget(edit_form.compania)}}</td>
            </tr>
            <tr>
                <th>Empleado</th>
                <td>{{form_widget(edit_form.empleado)}}</td>
            </tr>
        </table>
        <div style="display:none">{{form_rest(edit_form)}}</div>
        <a href="{{ path('companiacliente') }}" class="btn btn-info">
           Cancelar
        </a> <button type="submit" class="btn btn-primary"> <i class="fa fa-save"></i> Guardar</button>
     {{ form_end(edit_form) }}
    {% if is_granted('ROLE_ADMIN')%}
    {{ form(delete_form) }}
   {% endif %}
{% endblock %}
