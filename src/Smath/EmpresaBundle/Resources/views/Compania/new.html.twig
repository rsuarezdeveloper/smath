{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crear Compania</h1>

    {{ form(form) }}

        <div class="form-actions">
        <a href="{{ path('compania') }}" class="btn btn-mini">
            Cancelar        </a>
{% endblock %}
