{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Compania</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ entity.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ entity.telefono }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
        </tbody>
    </table>

        <div class="form-actions">
        <a href="{{ path('compania') }}" class="btn btn-mini">
            Cancelar        </a>
        <a href="{{ path('compania_edit', { 'id': entity.id }) }}" class="btn btn-primary btn-mini">
            Editar
        </a>
{{ form(delete_form) }}
{% endblock %}
